apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: continumm
spec:
  ingressClassName: nginx
  rules:
    - host: continumm.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx
                port:
                  number: 80

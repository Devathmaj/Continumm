# Environment Configuration Example
# Copy to .env and customize for your environment

# Application settings
PORT=8000
HOST=0.0.0.0
ENVIRONMENT=development
LOG_LEVEL=INFO

# Git commit (set automatically during build)
# GIT_COMMIT=<populated-during-build>
